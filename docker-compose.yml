services:
  backend:
    build:
      context: ./backend
    container_name: analizadorseguridadqr-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/app:/app/app
    restart: unless-stopped